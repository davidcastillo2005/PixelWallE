<Window x:Class="PixelWallE.NewCanvasSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PixelWallE"
        mc:Ignorable="d"
        Title="NewCanvasSetupWindow" Height="225" Width="400">
    <Grid
        Name="MainGrid"
        Loaded="MainGrid_Loaded">
        <Button
            Name="OKButton"
            Grid.Row="4"
            Grid.Column="0"
            Margin="5"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Width="75"
            Height="25"
            IsDefault="True"
            Click="OKButton_Click"
            Content="OK"/>
        <Button
            Name="CancelButton"
            Grid.Row="4"
            Grid.Column="1"
            Margin="5"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Width="75"
            Height="25"
            IsCancel="True"
            Click="CancelButton_Click"
            Content="Cancel"/>
        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            FontSize="12"
            FontWeight="Bold"
            TextWrapping="Wrap"
            Text="Height"
            Margin="5,5,5,5"/>
        <TextBlock
            Grid.Row="2"
            Grid.Column="0"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            FontSize="12"
            FontWeight="Bold"
            TextWrapping="Wrap"
            Text="Width"
            Margin="5,5,5,5"/>
        <TextBox
            Name="HeightTextBox"
            Grid.Row="1"
            Grid.Column="1"
            Margin="5"
            VerticalAlignment="Center"
            HorizontalAlignment="Left"
            MinWidth="50"
            MaxLines="1"
            Background="White"
            TextChanged="HeightTextBox_TextChanged"/>
        <TextBox
            Name="WidthTextBox"
            Grid.Row="2"
            Grid.Column="1"
            Margin="5"
            Background="White"
            VerticalAlignment="Center"
            HorizontalAlignment="Left"
            MinWidth="50"
            MaxLines="1"
            TextChanged="WidthTextBox_TextChanged"/>
        <Grid
            Name="PreviewGrid"
            Grid.Row="0"
            Grid.Column="3"
            Grid.RowSpan="5"
            Background="LightGray">
            <Border
                Name="PreviewBorder"
                Width="{Binding ElementName=MainGrid, Path=ActualHeight}"
                BorderBrush="Black"
                BorderThickness="2">
                <Canvas
                    Name="PreviewCanvas"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="White"/>
            </Border>
        </Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="*"/>
            <ColumnDefinition
                Width="*"/>
            <ColumnDefinition
                Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="*"/>
            <RowDefinition
                Height="auto"/>
            <RowDefinition
                Height="auto"/>
            <RowDefinition
                Height="*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
    </Grid>
</Window>
